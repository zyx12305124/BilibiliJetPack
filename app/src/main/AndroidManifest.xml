<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="learn.gouzi.bilibilijetpack">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".androidViewModel.AndroidViewModelActivity"></activity>
        <activity android:name=".viewModelSavedStateTest.SavedStateActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".scoreDemo.ScoreActivity"></activity>
        <activity android:name=".dataBindingTest.DataBindingTestActivity" />
        <activity android:name=".liveDataTest.LiveDataTestActivity" />
        <activity android:name=".viewModelTest.MainActivity" />
    </application>

</manifest>